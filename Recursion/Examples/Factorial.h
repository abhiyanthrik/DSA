#pragma once
#ifndef FACTORIAL_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define FACTORIAL_H

int factorial_of_n(int n);
int raised_ro_power_of_n(int m, int n);
#endif
